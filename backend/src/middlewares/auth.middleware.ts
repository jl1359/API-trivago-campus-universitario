/**
 * ARCHIVO: auth.middleware.ts
 * PROPÓSITO: Middleware para interceptar peticiones, leer el token JWT 
 * de la cabecera (Header) y validar si el usuario está autenticado y 
 * si tiene los permisos (Roles) necesarios para acceder a la ruta.
 */
