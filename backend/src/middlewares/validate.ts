/**
 * ARCHIVO: validate.ts
 * PROPÓSITO: Middleware genérico que recibe un Schema de Zod y 
 * valida que el body de la petición HTTP coincida con ese esquema.
 * Si no coincide, rechaza la petición con un error 400 Bad Request.
 */
