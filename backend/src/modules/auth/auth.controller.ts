/**
 * ARCHIVO: auth.controller.ts
 * PROPÓSITO: Recibir las peticiones HTTP (req, res).
 * Extrae los datos (ej: req.body.email, req.body.password),
 * llama a los métodos de auth.service.ts, y devuelve la respuesta HTTP (res.json).
 */
